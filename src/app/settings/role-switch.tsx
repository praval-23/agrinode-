import { router } from 'expo-router'; import { ScrollView, StyleSheet, Text } from 'react-native'; import { Card, PrimaryButton, SecondaryButton } from '@/components/agri/ui'; import { colors } from '@/theme/colors'; import { useApp } from '@/context/AppContext';
export default function RoleSwitch(){const {setRole}=useApp();return <ScrollView contentInsetAdjustmentBehavior="automatic" contentContainerStyle={s.page}><Text style={s.title}>Switch role</Text><Text style={s.copy}>Choose the workspace you need right now.</Text><Card><Text style={s.name}>Farmer workspace</Text><PrimaryButton label="Open farmer dashboard" onPress={()=>{setRole('farmer');router.replace('/farmer/dashboard')}}/></Card><Card><Text style={s.name}>Buyer workspace</Text><SecondaryButton label="Open buyer dashboard" onPress={()=>{setRole('buyer');router.replace('/buyer/dashboard')}}/></Card></ScrollView>}; const s=StyleSheet.create({page:{padding:24,gap:18,backgroundColor:colors.cream},title:{fontSize:28,fontWeight:'900',color:colors.ink},copy:{color:colors.muted},name:{fontSize:19,fontWeight:'800',color:colors.ink}});
